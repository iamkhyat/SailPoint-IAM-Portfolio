
# Diagrams

Visual companions to the write-ups elsewhere in this repo — same content, just easier to take in at a glance. Each one is linked inline from its corresponding markdown file, but they're listed here too in case you just want to browse them directly.

- `architecture-overview.svg` — end-to-end IAM architecture (HR → IdentityIQ → target apps)
- `jml-lifecycle-flow.svg` — Joiner / Mover / Leaver flow
- `app-onboarding-flow.svg` — IdentityIQ application onboarding, step by step
- `identity-attributes.svg` — how HR attributes drive role assignment and lifecycle events
- `provisioning-logic.svg` — identity change → role change → provisioning plan → execution
- `common-issues.svg` — JML troubleshooting triage map
- `iiq-certification-lifecycle.svg` — campaign design through audit trail
- `iiq-troubleshooting-layers.svg` — isolating aggregation vs. correlation vs. provisioning issues
- `aggregation-failure.svg` — aggregation pipeline and where it actually breaks
- `provisioning-errors.svg` — provisioning pipeline and where it actually breaks
- `certification-issues.svg` — certification campaign pipeline and where it actually breaks
- `entitlement-mapping-rbac.svg` — raw entitlements → schema → roles
- `idn-connector-architecture.svg` — IdentityNow sources, connectors, aggregation cadence
- `idn-access-profiles.svg` — access profile design and comparison to IdentityIQ roles
- `idn-transforms.svg` — data normalization and attribute generation in IdentityNow
